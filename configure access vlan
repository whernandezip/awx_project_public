---
- name: ios version output
  
  hosts: all
  
  connection: network_cli
  
  tasks:

 - name: Ensure GigabitEthernet1/0/7 is configured for access vlan 1132
  ios_l2_interface:
    name: GigabitEthernet1/0/7
    mode: access
    access_vlan: 1132
